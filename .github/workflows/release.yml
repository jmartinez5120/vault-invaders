name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Read version
        id: version
        run: echo "version=$(cat VERSION | tr -d '[:space:]')" >> "$GITHUB_OUTPUT"

      - name: Build release zip
        run: |
          NAME="vault-invaders-${{ steps.version.outputs.version }}"
          mkdir -p dist/$NAME
          cp vault_invaders.py install.sh LICENSE README.md VERSION dist/$NAME/
          cd dist
          zip -r "$NAME.zip" "$NAME"
          sha256sum "$NAME.zip" > "$NAME.zip.sha256"
          rm -rf "$NAME"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            dist/vault-invaders-${{ steps.version.outputs.version }}.zip
            dist/vault-invaders-${{ steps.version.outputs.version }}.zip.sha256
